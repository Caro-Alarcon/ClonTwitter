<p style="color: green"><%= notice %></p>

<h1>Tweets</h1>

<div class="my-5">
<%= form_with url: tweets_path, method: :get do |form|%>
  <%= form.text_field :query_text, placeholder: ('Escribe tu búsqueda'), value: params[:query_text]%>
  <%end%>
</div>

<div class="my-5 row">
    <% @tweets.each do |tweet| %>
      <div id="tweets" class="my-5 col-sm-6">
        <div class="card">
              <%= render tweet %>
              <p>
              <div class="d-grid gap-2 d-md-flex justify-content-md-end">
              <%= link_to "Show this tweet", tweet, class: "btn btn-info me-md-2"  %>
              </div>
              </p>
        </div>
      </div>
    <% end %>
    
<div class="d-grid gap-2 d-md-block">
  <%= link_to "New tweet", new_tweet_path, class: "btn btn-info"  %>
</div>

</div>

<div class="my-3 d-flex justify-content-center">
<%== pagy_bootstrap_nav(@pagy) %>
</div>